{
  "name": "pi-muse-code-context-fold",
  "version": "0.4.0",
  "description": "Registers the muse-code provider for Pi over MSP against a persistent `muse serve` host (not Muse OAuth), resuming a real muse session across turns instead of re-sending folded history text every time",
  "type": "module",
  "license": "MIT",
  "keywords": ["pi", "pi-extension", "muse-code"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zereight/pi-muse-code-context-fold.git"
  },
  "files": ["src", "agents", "README.md"],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@muse-code/sdk": "1.3.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  }
}
